<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="20dp">


    <de.hdodenhof.circleimageview.CircleImageView
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/sampleChatImage"
        android:layout_width="70dp"
        android:layout_height="70dp"
        android:src="@mipmap/ic_launcher"
        app:civ_border_width="2dp"
        android:layout_marginStart="8dp"
        android:layout_marginTop="8dp"
        android:padding="5dp"
        app:civ_border_color="#FF000000"/>

    <TextView
        android:id="@+id/sampleUserNameChats"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_toEndOf="@id/sampleChatImage"
        android:layout_marginTop="15dp"
        android:layout_marginStart="12dp"
        android:text="User Name"
        android:layout_centerHorizontal="true"
        android:textAllCaps="false"
        android:textColor="@color/black"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <TextView
        android:id="@+id/sampleLastMessageChats"
        android:textSize="15sp"
        android:layout_toEndOf="@id/sampleChatImage"
        android:layout_below="@id/sampleUserNameChats"
        android:layout_marginStart="12dp"
        android:text="LastMessage"
        android:layout_centerHorizontal="true"
        android:textAllCaps="false"
        android:textColor="@color/black"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

</RelativeLayout>